---
layout: post
title: "PHP 7.2: goodbye mcrypt, hello sodium"
date: 2018-01-14
categories: [php]
tags: [php, php72, security, cryptography]
---

PHP 7.2 released November 30th. The headline isn't a language feature — it's a removal. `mcrypt` is gone.

This is good news, even if it doesn't feel like it if you're the one migrating.

## :lock: The mcrypt problem

`mcrypt` has been unmaintained since 2007. More than a decade of stagnation in a cryptography library. It was deprecated in 7.1, and 7.2 removes it entirely. The replacement is `sodium`, now bundled as a core extension.

Sodium is the PHP binding for [libsodium](https://libsodium.org), a modern cryptographic library built around safe defaults. Where mcrypt required you to choose the right cipher, mode, and padding — and get it wrong silently — sodium's API makes dangerous choices structurally hard. `sodium_crypto_secretbox()` for symmetric encryption, `sodium_crypto_box()` for asymmetric, `sodium_crypto_sign()` for signatures. The names tell you what you're doing.

If you have mcrypt code in production, the migration is unavoidable. It's also worth doing carefully — cryptography code that "still works" can be silently broken.

## :package: The object type hint

7.2 adds `object` as a parameter and return type:

```php
function serialize(object $data): string {
    // accepts any object
}
```

It's broad — any object satisfies it — but it's better than no type at all when you genuinely don't care about the specific class. Complements the existing `array`, `callable`, and class-specific hints.

## :key: Argon2 in password_hash

```php
$hash = password_hash($password, PASSWORD_ARGON2I);
```

`PASSWORD_BCRYPT` was the default and still reasonable, but Argon2 won the Password Hashing Competition for a reason: it's memory-hard, making GPU-based cracking significantly more expensive. Worth switching for new applications.

7.2 is a security-focused release more than a language one. The removal of mcrypt and addition of sodium together make the platform more trustworthy for sensitive applications. The features are incremental — the infrastructure change is not.
